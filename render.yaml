services:
  - type: web
    name: dnf-hell-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python neople_hell_api_server.py --host 0.0.0.0
    healthCheckPath: /api/health
    envVars:
      - key: NEOPLE_API_KEY
        sync: false
