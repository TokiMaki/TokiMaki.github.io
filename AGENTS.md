# AGENTS.md

## 0. 최우선 원칙

> **First principle: Do not kill the model with a harness.**

제품을 만든다. 검증을 위한 장치를 만드는 일이 제품 구현보다 커지게 하지 않는다.

- 기존 테스트, API, 브라우저, 실제 UI, Electron, 확장 프로그램, 에뮬레이터 또는 기기로 확인할 수 있으면 그것을 사용한다.
- 기존 도구가 수행할 수 있는 검증을 대신하는 새 harness, runner, validator, 증거 수집 체계, mock 플랫폼 또는 대체 실행 환경을 만들지 않는다.
- 사소한 변경을 증명하기 위해 테스트 인프라, 계획 문서, 검증 스크립트 또는 추상화를 새로 만들지 않는다.
- 현재 도구로 제품 동작을 충분히 확인할 수 있으면 구현을 완료하고, 실제로 확인한 결과만 보고한다.
- 테스트 인프라 구축은 사용자가 명시적으로 요청했거나 제품 계약을 안전하게 지키기 위해 실제로 필요한 경우에만 별도 작업으로 취급한다.

이 원칙은 필수 검증을 생략하라는 뜻이 아니다. 보안, 권한, 개인정보, 금전 처리, 정산, 데이터 무결성, 동시성, 멱등성, 마이그레이션, 되돌릴 수 없는 작업, 외부 API, 공개 계약에서는 위험에 비례한 보호와 검증을 수행한다.

## 1. 기본 응답과 작업 태도

- 기본 응답은 한국어로 작성하고 사용자의 직접 질문에 먼저 답한다.
- 결론과 실제 영향을 먼저 설명하고, 기술적 세부사항은 그다음에 설명한다.
- 비개발자도 이해할 수 있는 표현을 사용하되 명령어, 식별자, 필드명, 오류 메시지는 정확히 보존한다.
- 리뷰·분석 요청에서는 구현을 명시하지 않은 한 코드를 수정하지 않는다.
- 구현 요청에서는 제품 계약을 만족하는 가장 작고 완전한 변경을 수행한다.
- 요청과 무관한 리팩터링, 파일 이동, 스타일 정리, 구조 변경은 하지 않는다.
- 확실하지 않은 내용은 추측하지 말고 관련 코드, 테스트, 계약과 실행 결과를 확인한다.
- 기존 미커밋 변경사항을 임의로 되돌리거나 덮어쓰지 않는다.
- 비밀키, 토큰, 개인정보를 코드·로그·응답에 노출하지 않는다.

## 2. 실용적 충분성과 위험 판단

구현 복잡도와 방어 수준은 관찰된 문제, 명시된 요구사항, 계약, 불변 조건, 테스트 실패와 구체적인 운영 위험에 맞춘다.

- 근거 없는 가상 엣지 케이스만을 이유로 로직, 추상화, 설정 또는 방어 코드를 추가하지 않는다.
- 위험을 발견하면 발생 조건, 가능성, 영향과 조치 필요성을 구분해서 설명한다.
- 가능성이나 영향이 낮으면 낮다고 명확히 말한다.
- 아직 발생하지 않았더라도 보안, 권한, 개인정보, 금전·정산, 데이터 무결성, 동시성·멱등성, 마이그레이션, 비가역 작업과 외부 API 경계에는 비례한 보호를 적용한다.
- 실용적 충분성을 이유로 필수 계약, 권한 검사, 트랜잭션, 데이터 검증 또는 필수 소비자 대응을 생략하지 않는다.

## 3. 프로젝트와 현재 방향

이 프로젝트는 **던파일럿(DunPilot)** 이다.

던전앤파이터 캐릭터의 현재 장비와 세팅을 분석하고, 가능한 스펙업 후보를 비교해 **골드 대비 효율이 높은 스펙업 순서**를 추천하는 서비스다.

> 핵심 질문: 현재 캐릭터에서 어떤 스펙업에 먼저 골드를 쓰는 것이 가장 효율적인가?

주요 범위는 네오플 API 기반 캐릭터 조회, 스펙업 후보 탐색, 딜·버프 상승량 계산, 경매장 가격·재료비 반영, 추천 row 생성과 정렬이다.

과거의 **헬 운영 최적화, 계시, 결정 조각, 솔리드 소울, 정가, 초월, 졸업 기대값**은 현재 기본 도메인이 아니다.

- `Docs/legacy/DNF_HELL_OPTIMIZER_SPEC.md`
- `계시 환산.xlsx`

위 자료는 사용자가 과거 기능을 명시적으로 요청한 경우에만 참고한다. 현재 스펙업 추천 로직의 근거로 자동 적용하지 않는다.

## 4. 목표 책임 경계

구조를 정리할 때 다음 책임 경계를 기준으로 한다.

- **API Client:** 네오플 API 호출과 응답 수신
- **Repository / Cache:** 캐시, TTL, single-flight, 묶음 조회, 실패 fallback
- **Candidate Resolver:** 비교 후보 탐색, 검색 조건, 후보 필터링
- **Calculator:** 딜, 버프, 비용 대비 효율 등 수치 계산
- **Presenter:** 프론트용 recommendation row와 payload 조립
- **Route:** HTTP 요청 파싱, 서비스 호출, 응답과 오류 코드 반환

현재 코드에 이 구조를 한 번에 강제하지 않는다.

- 한 번에 하나의 도메인만 분리한다.
- 클래스 계층이나 공통 인터페이스부터 만들지 않는다.
- 기존 흐름을 유지하면서 함수 이동 중심으로 최소 변경한다.
- 계산식과 추천 정책을 파일 분리 과정에서 다시 작성하지 않는다.
- 단순한 코드를 패턴이나 추상화로 감싸지 않는다.

## 5. 작업 시작과 탐색

코드 구현, 수정 또는 구조 검토 전에 관련된 실제 권위 자료만 확인한다. 권위 자료에는 현재 코드, 테스트, 계약, 호출부, 소비자와 사용자 화면이 포함된다.

코드 구현, 수정, 구조 검토를 시작할 때 `Docs/WORK_CONTEXT.md`가 있으면 먼저 `Active Snapshot`만 확인한다.

`src/dnfHellTool/enchantView.js` 분리 작업은 시작하거나 재개하기 전에 `Docs/architecture/ENCHANT_VIEW_REFACTOR_GOAL.md`를 반드시 읽고 목표·불변 조건·완료 기준을 다시 확인한다.

- 매번 문서 전체, 과거 로그 또는 관련 없는 파일을 읽지 않는다.
- 단순 질문이나 코드 변경이 없는 짧은 답변에서는 `WORK_CONTEXT`를 확인하지 않아도 된다.
- 파일이 없고 실제 구현 또는 구조 작업을 시작하는 경우에만 아래 최소 형식으로 생성한다.

```md
# WORK_CONTEXT

## Active Snapshot

- 현재 목표:
- 현재 주요 파일:
- 최근 변경:
- 중요한 결정/제약:
- 알려진 문제:
- 다음 작업:
```

코드 탐색은 다음 원칙을 따른다.

- 관련 함수명, 변수명, API 경로, payload 필드, UI 문구부터 검색한다.
- 검색 결과 주변 코드와 호출부를 확인한 뒤 필요한 경우에만 파일 전체를 읽는다.
- 관련 테스트, fixture, mock, 저장 응답과 실제 소비자가 있는지 확인한다.
- 중요한 호출부나 부작용을 놓칠 정도로 탐색 범위를 제한하지 않는다.
- persistence, migration, authorization, tenant, money, concurrency, idempotency, state transition, 외부 연동, 공개 계약 또는 cross-repository 변경에서는 미해결 제품 결정과 영향받는 소비자를 확인한다.
- 제품 결정이 이미 명확하면 불필요한 계획서나 리뷰 절차를 추가하지 않고 구현한다.

`Docs/QUESTION_LOG.md`는 자동으로 읽거나 생성·갱신하지 않는다. 사용자가 과거 결정 확인을 요청하고 파일이 있을 때만 필요한 부분을 확인한다.

레포지토리 전체 코드 리뷰에서는 `Docs/architecture/DUNPILOT_CODE_REVIEW.md`가 존재하면 해당 기준을 함께 따른다.

## 6. 멈춰야 하는 조건

다음 경우에만 구현을 멈추고 제품 결정을 요청한다.

- 필요한 권위 자료가 없어 올바른 동작을 판단할 수 없는 경우
- 구현하려면 새로운 제품 정책을 발명해야 하는 경우
- 선택에 따라 사용자 경험, 데이터 의미, 금전 또는 공개 계약이 실질적으로 달라지는 경우
- 요청 범위를 넘어서는 중대한 소비자 변경이나 마이그레이션이 필수인 경우

단순한 구현 선택, 네이밍, 내부 구조 또는 기존 패턴으로 해결 가능한 사항 때문에 작업을 멈추지 않는다. 권한, 트랜잭션, 데이터 무결성, 마이그레이션 또는 필수 소비자 작업을 조용히 약화하거나 생략하지 않는다.

## 7. 수정과 코드 품질

- 작은 diff를 우선하고 요청받은 문제에 필요한 코드만 수정한다.
- 작은 변경이 아니라 **작고 완전한 변경**을 목표로 한다. 필요한 호출부, 소비자, 오류 처리와 계약 반영은 함께 처리한다.
- 프로젝트의 기존 네이밍, import, 타입 힌트, 비동기 처리 스타일을 따른다.
- 함수와 변수 이름은 역할이 드러나게 작성한다.
- 중첩과 중복을 줄이되 이를 이유로 작업 범위를 넓히지 않는다.
- 의미 없는 임시 변수, 과도한 한 줄 표현, 불필요한 주석을 남기지 않는다.
- 주석은 코드가 무엇을 하는지보다 왜 필요한지를 설명할 때만 사용한다.
- 사용하지 않는 import, 변수, 죽은 코드를 새로 만들지 않는다.
- 관련 없는 파일의 포맷이나 줄바꿈을 대량 변경하지 않는다.
- 발견하지 않은 문제를 예방한다는 이유만으로 범용 abstraction, wrapper, adapter 또는 설정 계층을 추가하지 않는다.

사용자의 명시적 요청 없이는 다음을 변경하지 않는다.

- 계산식과 추천 기준
- 후보 필터링 조건
- 딜러/버퍼 분기
- 추천 개수와 정렬 순서
- payload 구조, 필드 의미, null 처리
- 가격과 재료비 단위
- 프론트 구현
- 저장 데이터 구조
- 외부 라이브러리와 프레임워크

코드에 근거가 없는 효과, 가격, 확률, 보정값을 임의로 만들거나 추정해서 넣지 않는다.

## 8. 동작 보존 리팩터링

구조 분리나 함수 이동은 가능하면 다음 순서로 진행한다.

1. 변경 전 동일 입력의 응답 또는 기존 fixture를 확보한다.
2. 대상 도메인 하나의 함수만 이동한다.
3. 기존 호출부를 최소 수정한다.
4. 같은 입력으로 재실행한다.
5. `debug`와 timing 값을 제외하고 결과를 비교한다.
6. 값, 개수, 정렬이 같을 때만 완료로 판단한다.

비교 대상에는 추천 개수·정렬 순서와 다음 핵심 필드가 포함된다.

`sourceType`, `slot`, `tier`, `itemName`, `effects`, `expectedGold`, `auction.minUnitPrice`, `materials`, `skillDamageMultiplier`, 서버가 제공하는 경우 `incrementalDamagePercent`

실시간 경매장 가격으로 결과가 흔들리면 기존 저장 응답, mock 또는 fixture를 사용한다. 이를 위해 새로운 대형 검증 체계를 만들지 않는다.

결과가 달라졌을 때 계산식이나 추천 기준을 바꿔 억지로 맞추지 않는다. 함수 이동, 입력 데이터, 캐시, 정렬 안정성, 누락된 의존성을 먼저 확인한다.

## 9. 검증과 제품 전달

제품 구현을 먼저 완료하고 변경 범위에 맞는 가장 좁고 신뢰할 수 있는 기존 검증 수단을 사용한다.

검증 우선순위는 다음과 같다.

1. 기존 테스트, 타입 검사, lint 또는 프로젝트 스크립트
2. 변경 함수와 실제 호출 흐름
3. 기존 fixture, mock 또는 저장 응답 비교
4. API 응답과 payload 값
5. 브라우저, 실제 UI, Electron, 확장 프로그램, 에뮬레이터 또는 기기
6. 필요한 경우에만 더 넓은 통합 검증

- UI를 변경했으면 가능할 때 실제 사용자 화면이나 브라우저로 확인한다.
- API를 변경했으면 실제 route와 소비자 계약을 확인한다.
- 기존 도구로 검증 가능한 일을 위해 대체 harness나 validator를 만들지 않는다.
- 요청하지 않은 대규모 전체 테스트, 장시간 검증 또는 불필요한 외부 API 호출은 하지 않는다.
- 변경 위험이 낮고 기존 좁은 검증이 통과하면 더 강한 증거를 만들기 위해 작업을 확대하지 않는다.
- 보안, 권한, 돈, 데이터 무결성, 동시성, 마이그레이션과 공개 계약 변경에는 위험에 맞는 검증을 반드시 수행한다.
- 검증할 수 없는 부분은 이유, 범위, 실제 위험을 명확히 적는다.
- 실행하지 않은 검증을 통과했다고 말하지 않는다.

## 10. WORK_CONTEXT와 문서 관리

`Docs/WORK_CONTEXT.md`는 현재 작업 상태를 다음 세션에 넘기기 위한 짧은 상태판으로 사용한다.

다음 경우에만 `Active Snapshot`을 갱신한다.

- 기능 구현 또는 중요한 버그 수정
- 계산식, 추천 기준, payload 계약 변경
- 구조 분리나 주요 파일 역할 변경
- 중요한 사용자 결정 반영
- 알려진 문제 또는 다음 작업 방향 변경

다음 경우에는 갱신하지 않는다.

- 단순 질문과 설명
- 코드 변경 없는 리뷰
- 사소한 포맷 수정
- 이미 기록된 내용의 반복

갱신 원칙:

- 기존 `Active Snapshot`을 최신 상태로 덮어써서 유지한다.
- 항목별 1~2줄로 짧게 쓰고 상세 작업 일지를 누적하지 않는다.
- 현재 목표, 주요 파일, 최근 변경, 중요한 결정·제약, 알려진 문제, 다음 작업만 남긴다.
- 민감정보, 긴 코드, 디버그 로그, 일회성 시행착오는 기록하지 않는다.
- `Session Log`는 사용자가 명시적으로 요청하지 않는 한 만들거나 누적하지 않는다.
- 구현보다 문서나 증거 작성이 더 커지지 않게 한다.

과거 헬 최적화 문서는 삭제하지 않더라도 현재 기능 문서와 명확히 구분한다. 지속적으로 필요한 계산 기준, payload 계약, 중요한 예외만 별도 문서화한다.

## 11. 종료 응답

최종 응답은 결론과 실제 영향을 먼저 말하고 짧게 작성한다.

- 답변하거나 변경한 내용
- 변경한 주요 파일
- 실제로 수행한 검증과 결과
- 확인하지 못한 사항 또는 남은 위험
- 후속 작업이 반드시 필요할 때만 한 가지 제안

위험을 보고할 때는 발생 조건, 가능성, 영향과 지금 조치가 필요한지를 구분한다. 낮은 위험을 중대한 문제처럼 표현하지 않는다.
